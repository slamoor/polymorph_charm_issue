
mainmodule hello {
    include "Interventions.h";
    mainchare Main {
        // Main's entry methods
        entry Main(CkArgMsg *m);
    };
    array [1D] Hello {
        // Hello's entry methods
        entry Hello();
        entry void SayHi(const BaseIntervention& b);
    };
};